//! Public surface for the stbox-viz examples and external consumers.
//! Currently exposes only `board3d` so the `board_debug` example can
//! sanity-check the renderer in isolation. Add other modules here on
//! demand.

pub mod board3d;
